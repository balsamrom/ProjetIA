# Django Framework
# Project uses Django 5.2.7 (compatible with Python 3.13)
# Supports both SQLite (default) and MySQL/MariaDB databases
Django>=5.2.0,<6.0

# Database Drivers
# PyMySQL for MySQL/MariaDB connection on Windows
PyMySQL>=1.0.0,<2.0.0

# Environment Variables
# python-dotenv for loading .env files
python-dotenv>=1.0.0

# Machine Learning & Data Science
# scikit-learn for ML models (TfidfVectorizer, pipelines)
scikit-learn>=1.0.0,<2.0.0

# pandas for data manipulation
pandas>=1.3.0,<3.0.0

# numpy for numerical computing
numpy>=1.21.0,<2.0.0

# joblib for loading/saving ML models (.pkl files)
joblib>=1.0.0,<2.0.0

# HTTP Requests
# requests for API calls (optional but used in views)
requests>=2.28.0

# OpenAI API (optional)
# openai for GPT integration (optional, gracefully handles if not installed)
# Note: The code handles ImportError gracefully, so this is optional
openai>=0.27.0

# Pillow for ImageField support
# Required for Django ImageField in Event and Hotel models
Pillow>=9.0.0,<11.0.0

